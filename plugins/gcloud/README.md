# Google Cloud SDK autocompletion
Plugin that will simply load the autocompletion that should be included in every gcloud SDK
installation.

To use it, add `gcloud` to the plugins array in your zshrc file.

```
plugins=(... gcloud)
```

**Author:** [@HenningCash](https://github.com/HenningCash)
